- op: replace
  path: /base/jenkins
  values: |
    controller:
      nodePort: 31469
    persistence:
      enabled: true
      existingClaim: nfs-pvc-b
      storageClass: local-path
      accessMode: "ReadWriteMany"
      size: 5Gi 
